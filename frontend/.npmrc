# Do NOT add platform-specific packages (e.g. @tailwindcss/oxide-win32-x64-msvc)
# directly to package.json. They should only be optionalDependencies of their
# parent package — npm will select the right one for the current OS automatically.
